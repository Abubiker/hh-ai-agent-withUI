# Инструменты только для разработки — не тянуть в собранный .app
# (build_app.sh / HHAgent.spec ставят зависимости из requirements.txt).
-r requirements.txt
pytest
pytest-asyncio
numpy  # только для make_icon.py — вырезает буквы логотипа по цвету
